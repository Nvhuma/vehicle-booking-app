import React from 'react'

function file() {

	const blocks = (int, component) => {
			// print the component for the number of in entered
	}

	return (
		<div className="board-container">
			
		</div>
	)
}

export default file